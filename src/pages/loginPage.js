import React from "react";
import PageTemplate from "../components/loginForm";
import AddToFavouritesIcon from '../components/cardIcons/addToFavourites'

const LoginPage = (props) => {

  return (
    <PageTemplate />

  );
};

export default LoginPage;